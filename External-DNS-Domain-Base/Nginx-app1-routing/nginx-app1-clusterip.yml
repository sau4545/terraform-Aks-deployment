apiVersion: v1
kind: Service
metadata:
  name: myapp1-nginx-clusterip-service
  labels:
    app: nginx-app1
spec:
  type: ClusterIP
  selector:
    app: nginx-app1
  ports:
  - port: 80
    targetPort: 80
